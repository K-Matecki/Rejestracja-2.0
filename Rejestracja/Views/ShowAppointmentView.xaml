<Page x:Class="Rejestracja.Views.ShowAppointmentView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Rejestracja.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ShowAppointmentView">

    <Grid>
        <Grid HorizontalAlignment="Left" Height="430" Margin="10,10,0,0" VerticalAlignment="Top" Width="780">
            <Label Content="Wybierz date" HorizontalAlignment="Left" Margin="345,65,0,0" VerticalAlignment="Top"/>
            <DatePicker HorizontalAlignment="Left" Margin="255,110,0,0" VerticalAlignment="Top" Height="55" Width="260"/>
            <DataGrid Name="AppointmentTable" HorizontalAlignment="Left" Height="205" Margin="115,195,0,0" VerticalAlignment="Top" Width="550">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding  AppointmentDate}" />
                <DataGridTextColumn Header="Imie Lekarza" Binding="{Binding Surname}" />
                <DataGridTextColumn Header="Nazwisko Lekarza" Binding="{Binding }" />
                <DataGridTextColumn Header="Imie pacjenta" Binding="{Binding }" />
                <DataGridTextColumn Header="Nazwisko pacjenta" Binding="{Binding }" />
            </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Page>
